# Update iOS Architecture Diagram

Analyze the current iOS codebase and update the Mermaid architecture diagram in `ios/ARCHITECTURE.md` to reflect the current structure, components, and data flow. Also update the file tree and key architecture points in `ios/CLAUDE.md` if needed.
