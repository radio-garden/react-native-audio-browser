import { NativeAudioBrowser } from './web/NativeAudioBrowser'

/**
 * Web implementation: AudioBrowser is a unified browser + player.
 * @internal
 */
export const nativeBrowser = new NativeAudioBrowser()
