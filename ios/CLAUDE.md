# iOS Development Guide

## Quick Commands

- `yarn ios:rebuild` - Rebuild and run with pod install (required after adding/removing files)
- `yarn ios:format` - Format Swift code

## Architecture

See [ARCHITECTURE.md](ARCHITECTURE.md) for the full mermaid dependency diagram.

## Key Architecture Points

### Nitro Module Pattern

- `HybridAudioBrowser` is the single entry point, implementing `HybridAudioBrowserSpec`
- Unified interface for both browsing and playback functionality
- Direct JS-native method calls (no event emitters)
- ~50 callbacks as closure properties organized into categories:
  - **Browser** (4): `onPathChanged`, `onContentChanged`, `onTabsChanged`, `onNavigationError`
  - **Player** (14): `onPlaybackProgressUpdated`, `onPlaybackActiveTrackChanged`, `onPlaybackPlayingState`, etc.
  - **Remote** (16): `onRemotePlay`, `onRemotePause`, `onRemoteNext`, etc.
  - **Remote Handlers** (14): `handleRemotePlay`, `handleRemotePause`, etc. (optional JS overrides)
  - **Other** (8): `onOptionsChanged`, `onFavoriteChanged`, `onNowPlayingChanged`, `onOnlineChanged`, etc.
- Types generated by Nitrogen in `nitrogen/generated/ios/swift/`

### Playback Core: TrackPlayer + PlaybackCoordinator

The playback core is split into two `@MainActor` types:

- **TrackPlayer** is the AVPlayer bridge. It owns the `AVPlayer`, the four observers, `MediaLoader`,
  `NowPlayingUpdater`, `RemoteCommandController`, `NowPlayingInfoController`, `RetryManager`, and
  `PlaybackStateStore`. It conforms to `PlaybackEffectHandler` (so the coordinator can drive
  AVPlayer), `MediaLoaderDelegate`, `SeekCompletionHandler`, and `TrackSelectionPlayer`.
- **PlaybackCoordinator** (owned by TrackPlayer) is the **state-machine hub**. It holds the
  `PlaybackState`, runs `transition(_ event:)`, applies side effects through the
  `PlaybackEffectHandler` (TrackPlayer), and fires JS-facing callbacks through
  `PlaybackCoordinatorCallbacks` (HybridAudioBrowser). It owns `QueueManager`, `PlaybackErrorHandler`,
  `SleepTimerManager`, `LoadSeekCoordinator`, `VolumeFader`, `PlayingStateManager`, and two
  `PlaybackTimer` instances (progress + interval).

The state machine itself is a **pure function**: `nextPlaybackState(from:on:)` in
`PlaybackStateMachine.swift` is a transition table over the `PlaybackEvent` enum (`PlaybackEvent.swift`).
State-related guards live in the table; context-related guards (`asset != nil`, `nearTrackEnd`, etc.)
live at the call site that decides whether to fire the event.

`TrackPlayerCallbacks` **extends** `PlaybackCoordinatorCallbacks`, so HybridAudioBrowser implements
both with one conformance: the coordinator's Nitro-typed callbacks plus TrackPlayer's metadata/seek/
remote/config callbacks.

### Data Flow

1. **JS → Native**: Direct sync/async method calls via Nitro
2. **Native → JS**: Callback properties invoked from native code (TrackPlayer/coordinator events route through HybridAudioBrowser)
3. **Browser → Player**: `navigateTrack()` uses TrackSelector to resolve track selection (contextual URL expansion, queue reuse, handler interception) and then executes the resulting PlaybackIntent
4. **State Machine**: PlaybackEvent triggers a deterministic transition via `PlaybackCoordinator.transition(_ event)`; the coordinator queries `nextPlaybackState(from:on:)`, applies side effects through the `PlaybackEffectHandler` (TrackPlayer drives AVPlayer), updates timers/playing-state, and emits callbacks
5. **Coordinator → Queue**: PlaybackCoordinator delegates queue operations to QueueManager (pure state, no AVPlayer knowledge) and conforms to `QueueManagerDelegate`
6. **Coordinator → Error/Retry**: PlaybackErrorHandler classifies errors and orchestrates retries via `RetryHandling` (RetryManager), which is network-aware via NetworkMonitor
7. **Player → MediaLoader**: TrackPlayer delegates URL resolution and asset loading to MediaLoader; receives results via MediaLoaderDelegate
8. **Player → NowPlayingUpdater**: TrackPlayer calls NowPlayingUpdater for track metadata and artwork; NowPlayingUpdater updates NowPlayingInfoController (playback dynamics auto-publish via MPNowPlayingSession)
9. **Coordinator → LoadSeekCoordinator**: deferred seeks that arrive before the AVPlayerItem is ready are captured and replayed; completion arrives via SeekCompletionHandler (TrackPlayer)
10. **Platform → Observers**: KVO and NotificationCenter feed back to TrackPlayer, which translates them into PlaybackEvents

### Thread Safety

- `HybridAudioBrowser.onMainActor()` ensures player operations run on main thread
- `TrackPlayer`, `PlaybackCoordinator`, `BrowserManager`, `TrackSelector`, all observers, controllers, and coordinator-owned managers are `@MainActor`
- `PlaybackStateStore`, `ShuffleOrder`, and the judgement/criteria value types are plain (non-`@MainActor`) value/data types
- `LRUCache` uses NSLock for thread-safe access; `HttpClient` and `NetworkMonitor` are `@unchecked Sendable`
- `UncheckedSendableBox` wrapper for non-Sendable return types from `onMainActor()` calls

### Relationship to RNTP

This codebase is adapted from react-native-track-player. Key differences:

- **Bridge layer removed** - No `TrackPlayerModule.swift`, no Obj-C bridge
- **Nitro types used** - `Track`, `PlayingState`, events from nitrogen/generated
- **Browser added** - Navigation, routing, HTTP client (not in RNTP)
- **Unified API** - Browser and player merged into single HybridAudioBrowser
- **URL Resolution** - Media and artwork URL transforms via configuration

## Project Structure

```
ios/
├── HybridAudioBrowser.swift          # Main Nitro entry point
│                                     # Implements HybridAudioBrowserSpec & TrackPlayerCallbacks
│                                     # ~50 callback properties + ~12 Emitters (browser, player, remote)
│                                     # Static `shared` reached by Siri intent handlers
├── TrackPlayer.swift                 # AVPlayer bridge
│                                     # Owns PlaybackCoordinator, observers, MediaLoader,
│                                     #   NowPlayingUpdater, RemoteCommandController,
│                                     #   NowPlayingInfoController, RetryManager, PlaybackStateStore
│                                     # Conforms to PlaybackEffectHandler, MediaLoaderDelegate,
│                                     #   SeekCompletionHandler, TrackSelectionPlayer
├── PlaybackEvent.swift               # PlaybackEvent enum (transition triggers)
├── TrackSelector.swift               # @MainActor shared track selection decision tree
│                                     # Used by HybridAudioBrowser and CarPlayController
│                                     # Returns SelectionResult enum (play/intercepted/browse/none)
│                                     # Handles contextual URL expansion, queue reuse, handler interception
├── TrackPlayerCallbacks.swift        # @MainActor callback protocol (extends PlaybackCoordinatorCallbacks)
│                                     # Bridge between TrackPlayer events and HybridAudioBrowser
├── Browser/
│   ├── BrowserManager.swift          # @MainActor navigation, routing, caching, favorites
│   ├── BrowserManager+URLResolution.swift  # resolveMediaUrl / resolveArtworkUrl (layered transforms)
│   ├── SimpleRouter.swift            # Route pattern matching ({param}, *, ** wildcards)
│   ├── BrowserConfig.swift           # Config wrapper + JS handlers (track load, nav error, album url)
│   ├── BrowserPathHelper.swift       # Path utilities & contextual URLs (__trackId encoding)
│   ├── MediaResolveComposer.swift    # Pure URL layer composition (Nitro-free, testable)
│   ├── SearchDrillIn.swift           # Browsable-first drill-in for "play X" voice intents
│   ├── TabBarEntries.swift           # Tab-bar churn suppression (same(old:new:))
│   └── JsonModels.swift              # JSON Codable models for API responses
├── Observer/
│   ├── PlayerStateObserver.swift     # @MainActor KVO: AVPlayer.status, timeControlStatus
│   ├── PlayerTimeObserver.swift      # Audio-start boundary observer (1ms)
│   ├── PlayerItemNotificationObserver.swift  # Track end/fail/stalled notifications
│   └── PlayerItemPropertyObserver.swift      # Duration, buffering, timed metadata (+ metadata output)
├── Player/
│   ├── PlaybackCoordinator.swift     # @MainActor state-machine hub (owned by TrackPlayer)
│   │                                 # Holds PlaybackState, runs transition(_ event:), side effects
│   │                                 # Owns QueueManager, PlaybackErrorHandler, SleepTimerManager,
│   │                                 #   LoadSeekCoordinator, VolumeFader, PlayingStateManager,
│   │                                 #   progressTimer + intervalTimer (PlaybackTimer)
│   │                                 # Drives AVPlayer via PlaybackEffectHandler; conforms to QueueManagerDelegate
│   ├── PlaybackCoordinatorCallbacks.swift  # @MainActor protocol: coordinator → JS callbacks (Nitro types)
│   ├── PlaybackStateMachine.swift    # Pure transition table nextPlaybackState(from:on:)
│   ├── PlaybackEffectHandler.swift   # @MainActor protocol: AVPlayer side effects (impl: TrackPlayer)
│   ├── PlaybackErrorHandler.swift    # @MainActor error classify + retry orchestration; RetryHandling protocol
│   ├── PlaybackStateStore.swift      # UserDefaults cold-start resume persistence
│   ├── PlaybackTimer.swift           # @MainActor interval timer (progress + playback interval)
│   ├── VolumeFader.swift             # @MainActor sleep-timer fade-out
│   ├── PlayerStatusTypes.swift       # Player status value types
│   ├── QueueManager.swift            # @MainActor queue state, navigation, repeat, shuffle
│   │                                 # Delegate: QueueManagerDelegate (impl: PlaybackCoordinator)
│   ├── ShuffleOrder.swift            # Fisher-Yates shuffle ordering (value type)
│   ├── MediaLoader.swift             # @MainActor URL resolution, AVURLAsset, metadata/playable loading
│   ├── MediaLoaderDelegate.swift     # @MainActor protocol: item ready, errors, metadata callbacks
│   ├── NowPlayingUpdater.swift       # @MainActor now-playing metadata + artwork (Kingfisher)
│   │                                 # Updates NowPlayingInfoController (dynamics auto-published)
│   ├── LoadSeekCoordinator.swift     # Deferred seek state machine (idle/pendingSeek/seekInFlight)
│   ├── SeekCompletionHandler.swift   # @MainActor protocol: handleSeekCompleted(to:didFinish:)
│   ├── PlayingStateManager.swift     # Computes playing/buffering state
│   ├── SleepTimerManager.swift       # @MainActor sleep timer (time & end-of-track); SleepTimerHandling
│   ├── SleepTimerHandling.swift      # Abstract sleep-timer protocol (coordinator dependency)
│   ├── RetryManager.swift            # @MainActor exponential backoff, network-aware; RetryHandling
│   ├── AirPlayStallJudgement.swift   # Pure judgement: AirPlay stall classification
│   ├── EndOfTrackJudgement.swift     # Pure judgement: end-of-track detection
│   └── MediaIntentCriteria.swift     # Siri INPlayMediaIntent → search criteria mapping
├── NowPlayingInfo/
│   ├── NowPlayingInfoController.swift # @MainActor MPNowPlayingSession + nowPlayingInfo metadata
│   ├── NowPlayingInfoKeyValue.swift  # Key-value protocol
│   ├── MediaItemProperty.swift       # Track metadata properties
│   └── NowPlayingInfoProperty.swift  # Playback state properties
├── RemoteCommand/
│   └── RemoteCommandController.swift # @MainActor MPRemoteCommandCenter handlers
│                                     # Lazy handler properties; iOS 16+ session command-center switching
├── CarPlay/
│   ├── CarPlayController.swift       # @MainActor @objc(RNABCarPlayController)
│   │                                 # CPTabBar/CPList templates, navigation
│   │                                 # Owns CarPlayImageLoader, CarPlayListItemFactory,
│   │                                 #   CarPlayNowPlayingManager, TrackSelector
│   ├── CarPlayArtworkResolver.swift   # Platform-independent artwork load strategy (enum)
│   ├── CarPlayImageLoader.swift      # @MainActor SF Symbols, artwork loading, adaptive tinting
│   ├── CarPlayListItemFactory.swift  # @MainActor CPListItem / CPListSection builder
│   ├── CarPlayNowPlayingManager.swift # @MainActor Now Playing, buttons, Up Next, NowPlayingObserver
│   ├── ImageRowItem+CarPlay.swift    # ImageRowItem → Track adapter for CarPlay reuse
│   ├── RNABMediaIntentHandler.swift  # Siri INPlayMediaIntent → handlePlayMediaIntent
│   ├── RNABMediaAddHandler.swift     # Siri INAddMediaIntent → setActiveTrackFavorited(true)
│   ├── RNABMediaAffinityHandler.swift # Siri INUpdateMediaAffinityIntent → like/dislike favorite
│   ├── RNABAudioBrowser.h/.m         # ObjC glue: vends Siri intent handlers (handlerForIntent:)
│   └── RNABCarPlaySceneDelegate.h/.m # ObjC CPTemplateApplicationSceneDelegate; owns CarPlayController
├── Http/
│   └── HttpClient.swift              # URLSession wrapper (JSON decoding with detailed errors)
├── Model/
│   ├── TrackPlayerError.swift        # PlaybackError, QueueError
│   ├── MediaURL.swift                # URL parsing utilities
│   ├── RemoteCommand.swift           # Remote command enum with config
│   ├── SourceType.swift              # file vs stream detection
│   ├── PlayerUpdateOptions.swift     # Update options struct
│   └── NitroTypeStubs.swift          # Nitro type stubs
├── Extension/
│   ├── Track+AVPlayer.swift          # Track artwork loading
│   ├── Track+Copying.swift           # Track.copying() for selective field updates
│   ├── ResolvedTrack+Copying.swift   # ResolvedTrack.copying()
│   ├── RequestConfig+Copying.swift   # RequestConfig.copying()
│   ├── Capability+RemoteCommand.swift # Capability → RemoteCommand conversion
│   ├── PlayerCapabilities+Favorite.swift # Favorite capability helpers
│   ├── ChapterMetadata+AVFoundation.swift  # AVTimedMetadataGroup → ChapterMetadata
│   ├── TimedMetadata+AVFoundation.swift    # AVTimedMetadataGroup → TimedMetadata
│   ├── TrackMetadata+AVFoundation.swift    # AVMetadataItem → TrackMetadata
│   ├── NavigationError+Conversion.swift   # Error → NavigationError (shared by HAB & CarPlayController)
│   └── CxxVectorConformances.swift   # Nitro C++ vector type conformances
├── Option/
│   ├── PitchAlgorithms.swift         # AVAudioTimePitchAlgorithm mapping
│   ├── TimeEventFrequency.swift      # Event frequency enum
│   └── SessionCategories.swift       # Audio session categories
├── Util/
│   ├── LRUCache.swift                # Thread-safe LRU cache (O(1) get/set, doubly-linked list)
│   ├── MetadataAdapter.swift         # AVMetadataItem parsing
│   ├── Emitter.swift                 # Generic multi-listener event emitter
│   ├── OnceValue.swift               # One-time async initialization gate (cold start)
│   ├── NetworkMonitor.swift          # NWPathMonitor wrapper
│   ├── ArtworkImageFetcher.swift     # @MainActor Kingfisher fetch (SVG / headers support)
│   ├── SFSymbolRenderer.swift        # SF Symbol → file:// image rendering
│   └── SVGProcessor.swift            # SVG → UIImage (SwiftDraw Kingfisher processor)
├── Tests/                            # XCTest unit tests (router, queue, seek, media loader,
│   │                                 #   state machine, coordinator, error handler, judgements, etc.)
│   └── Support/
│       └── TestTypes.swift           # Test support types
└── Support/
    └── RNTrackPlayer-Bridging-Header.h  # Objective-C bridging header
```

## Type Mapping

Nitro generates types that must be used instead of RNTP types:

| Nitro Type (use this)          | RNTP Type (removed)                            |
| ------------------------------ | ---------------------------------------------- |
| `Track`                        | `ios/Model/Track.swift`                        |
| `PlayingState`                 | `ios/Model/PlaybackPlayingState.swift`         |
| `RepeatMode`                   | `ios/Option/RepeatMode.swift`                  |
| `PlaybackProgressUpdatedEvent` | `ios/Event/PlaybackProgressUpdatedEvent.swift` |
| etc.                           | etc.                                           |

## Implementation Notes

### Track Type

The Nitro `Track` has:

- `url: String?` - Browsable path (for navigation)
- `src: String?` - Playback URL (for AVPlayer)
- `artwork: String?` - Artwork URL
- `artworkSource: ImageSource?` - Detailed artwork config

Use `track.src ?? track.url` for AVPlayer URL.

### Callbacks

HybridAudioBrowserSpec defines ~50 callbacks as properties:

```swift
// Browser callbacks (4)
var onPathChanged: (String) -> Void = { _ in }
var onContentChanged: (ResolvedTrack?) -> Void = { _ in }
var onTabsChanged: ([Track]) -> Void = { _ in }
var onNavigationError: (NavigationErrorEvent) -> Void = { _ in }

// Playback callbacks (14)
var onPlaybackProgressUpdated: (PlaybackProgressUpdatedEvent) -> Void = { _ in }
var onPlaybackActiveTrackChanged: (PlaybackActiveTrackChangedEvent) -> Void = { _ in }
var onPlaybackPlayingState: (PlayingState) -> Void = { _ in }
var onPlaybackError: (PlaybackErrorEvent) -> Void = { _ in }
var onPlaybackChanged: (Playback) -> Void = { _ in }
// ...etc

// Remote callbacks (16) - fire events to JS
var onRemotePlay: () -> Void = {}
var onRemotePause: () -> Void = {}
var onRemoteNext: () -> Void = {}
// ...etc

// Remote handlers (14) - optional JS overrides for default behavior
var handleRemotePlay: (() -> Void)?     // If set, overrides default
var handleRemotePause: (() -> Void)?
var handleRemoteNext: (() -> Void)?
// ...etc
```

Callbacks are set from:

- `TrackPlayerCallbacks` protocol methods (player events)
- `BrowserManager` callbacks (navigation events)
- `NetworkMonitor.onChanged` (connectivity)
- `SleepTimerManager.onChanged` (sleep timer)

### Promises

Async methods return `Promise<T>`:

```swift
func setupPlayer(options: NativeSetupPlayerOptions) throws -> Promise<Void>
```

Use Nitro's Promise helpers for async operations.

#### Chaining `.then`/`.catch` on a JS-callback Promise from an `@MainActor` type

When you invoke a JS callback that returns a `Promise<T>` (a `formatNavigationError` / now-playing
formatter, etc.) and chain `.then`/`.catch`, **Nitro resolves that promise on the JS thread**, not
on main. `Promise.then(_:)` takes a non-`@Sendable` closure, so inside an `@MainActor` type the
resolver is *inferred* `@MainActor`-isolated — and running a main-isolated closure off-main traps:
`_swift_task_checkIsolated` → `dispatch_assert_queue_fail` (crashes on the
`com.facebook.react.runtime.JavaScript` thread).

Mark the resolver `@Sendable` to break the isolation inheritance, then hop back to the main actor
for any main-isolated work:

```swift
formatter(params)
  .then { @Sendable [weak self] result in
    guard let self, let result else { return }
    Task { @MainActor in self.apply(result) }
  }
  .catch { @Sendable error in
    let message = error.localizedDescription      // compute off-main; only a String crosses
    Task { @MainActor in self?.logger.error("formatter failed: \(message)") }
  }
```

This is the same reason the artwork callback in `NowPlayingUpdater` is `@Sendable`. A direct
`self?.someMainActorMethod(...)` call inside `.then` only *appears* to work when that particular
promise happens to resolve on main — it's a latent crash otherwise.
**`CarPlayController.showNavigationError` currently has that latent shape; don't copy it.**

### Logging

**Always use `os.Logger` instead of `print()` for debugging.**

```swift
import os.log

class MyClass {
    private let logger = Logger(subsystem: "com.audiobrowser", category: "MyClass")

    func doSomething() {
        logger.debug("Debug message")
        logger.info("Info message")
        logger.error("Error message")
    }
}
```

- Use `logger.debug()` for development debugging
- Use `logger.info()` for general information
- Use `logger.error()` for errors
- See `HttpClient.swift` for a complete example
