///
/// JPlaybackActiveTrackChangedEvent.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2026 Marc Rousavy @ Margelo
///

#pragma once

#include <fbjni/fbjni.h>
#include "PlaybackActiveTrackChangedEvent.hpp"

#include "HttpMethod.hpp"
#include "ImageSource.hpp"
#include "JHttpMethod.hpp"
#include "JImageSource.hpp"
#include "JTrack.hpp"
#include "JTrackStyle.hpp"
#include "Track.hpp"
#include "TrackStyle.hpp"
#include <optional>
#include <string>
#include <unordered_map>

namespace margelo::nitro::audiobrowser {

  using namespace facebook;

  /**
   * The C++ JNI bridge between the C++ struct "PlaybackActiveTrackChangedEvent" and the the Kotlin data class "PlaybackActiveTrackChangedEvent".
   */
  struct JPlaybackActiveTrackChangedEvent final: public jni::JavaClass<JPlaybackActiveTrackChangedEvent> {
  public:
    static auto constexpr kJavaDescriptor = "Lcom/margelo/nitro/audiobrowser/PlaybackActiveTrackChangedEvent;";

  public:
    /**
     * Convert this Java/Kotlin-based struct to the C++ struct PlaybackActiveTrackChangedEvent by copying all values to C++.
     */
    [[maybe_unused]]
    [[nodiscard]]
    PlaybackActiveTrackChangedEvent toCpp() const {
      static const auto clazz = javaClassStatic();
      static const auto fieldLastIndex = clazz->getField<jni::JDouble>("lastIndex");
      jni::local_ref<jni::JDouble> lastIndex = this->getFieldValue(fieldLastIndex);
      static const auto fieldLastTrack = clazz->getField<JTrack>("lastTrack");
      jni::local_ref<JTrack> lastTrack = this->getFieldValue(fieldLastTrack);
      static const auto fieldLastPosition = clazz->getField<double>("lastPosition");
      double lastPosition = this->getFieldValue(fieldLastPosition);
      static const auto fieldIndex = clazz->getField<jni::JDouble>("index");
      jni::local_ref<jni::JDouble> index = this->getFieldValue(fieldIndex);
      static const auto fieldTrack = clazz->getField<JTrack>("track");
      jni::local_ref<JTrack> track = this->getFieldValue(fieldTrack);
      return PlaybackActiveTrackChangedEvent(
        lastIndex != nullptr ? std::make_optional(lastIndex->value()) : std::nullopt,
        lastTrack != nullptr ? std::make_optional(lastTrack->toCpp()) : std::nullopt,
        lastPosition,
        index != nullptr ? std::make_optional(index->value()) : std::nullopt,
        track != nullptr ? std::make_optional(track->toCpp()) : std::nullopt
      );
    }

  public:
    /**
     * Create a Java/Kotlin-based struct by copying all values from the given C++ struct to Java.
     */
    [[maybe_unused]]
    static jni::local_ref<JPlaybackActiveTrackChangedEvent::javaobject> fromCpp(const PlaybackActiveTrackChangedEvent& value) {
      using JSignature = JPlaybackActiveTrackChangedEvent(jni::alias_ref<jni::JDouble>, jni::alias_ref<JTrack>, double, jni::alias_ref<jni::JDouble>, jni::alias_ref<JTrack>);
      static const auto clazz = javaClassStatic();
      static const auto create = clazz->getStaticMethod<JSignature>("fromCpp");
      return create(
        clazz,
        value.lastIndex.has_value() ? jni::JDouble::valueOf(value.lastIndex.value()) : nullptr,
        value.lastTrack.has_value() ? JTrack::fromCpp(value.lastTrack.value()) : nullptr,
        value.lastPosition,
        value.index.has_value() ? jni::JDouble::valueOf(value.index.value()) : nullptr,
        value.track.has_value() ? JTrack::fromCpp(value.track.value()) : nullptr
      );
    }
  };

} // namespace margelo::nitro::audiobrowser
