///
/// JTrackRequest.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#include <fbjni/fbjni.h>
#include "TrackRequest.hpp"

#include <optional>
#include <string>
#include <unordered_map>

namespace margelo::nitro::audiobrowser {

  using namespace facebook;

  /**
   * The C++ JNI bridge between the C++ struct "TrackRequest" and the the Kotlin data class "TrackRequest".
   */
  struct JTrackRequest final: public jni::JavaClass<JTrackRequest> {
  public:
    static auto constexpr kJavaDescriptor = "Lcom/margelo/nitro/audiobrowser/TrackRequest;";

  public:
    /**
     * Convert this Java/Kotlin-based struct to the C++ struct TrackRequest by copying all values to C++.
     */
    [[maybe_unused]]
    [[nodiscard]]
    TrackRequest toCpp() const {
      static const auto clazz = javaClassStatic();
      static const auto fieldUserAgent = clazz->getField<jni::JString>("userAgent");
      jni::local_ref<jni::JString> userAgent = this->getFieldValue(fieldUserAgent);
      static const auto fieldHeaders = clazz->getField<jni::JMap<jni::JString, jni::JString>>("headers");
      jni::local_ref<jni::JMap<jni::JString, jni::JString>> headers = this->getFieldValue(fieldHeaders);
      static const auto fieldQuery = clazz->getField<jni::JMap<jni::JString, jni::JString>>("query");
      jni::local_ref<jni::JMap<jni::JString, jni::JString>> query = this->getFieldValue(fieldQuery);
      return TrackRequest(
        userAgent != nullptr ? std::make_optional(userAgent->toStdString()) : std::nullopt,
        headers != nullptr ? std::make_optional([&]() {
          std::unordered_map<std::string, std::string> __map;
          __map.reserve(headers->size());
          for (const auto& __entry : *headers) {
            __map.emplace(__entry.first->toStdString(), __entry.second->toStdString());
          }
          return __map;
        }()) : std::nullopt,
        query != nullptr ? std::make_optional([&]() {
          std::unordered_map<std::string, std::string> __map;
          __map.reserve(query->size());
          for (const auto& __entry : *query) {
            __map.emplace(__entry.first->toStdString(), __entry.second->toStdString());
          }
          return __map;
        }()) : std::nullopt
      );
    }

  public:
    /**
     * Create a Java/Kotlin-based struct by copying all values from the given C++ struct to Java.
     */
    [[maybe_unused]]
    static jni::local_ref<JTrackRequest::javaobject> fromCpp(const TrackRequest& value) {
      using JSignature = JTrackRequest(jni::alias_ref<jni::JString>, jni::alias_ref<jni::JMap<jni::JString, jni::JString>>, jni::alias_ref<jni::JMap<jni::JString, jni::JString>>);
      static const auto clazz = javaClassStatic();
      static const auto create = clazz->getStaticMethod<JSignature>("fromCpp");
      return create(
        clazz,
        value.userAgent.has_value() ? jni::make_jstring(value.userAgent.value()) : nullptr,
        value.headers.has_value() ? [&]() -> jni::local_ref<jni::JMap<jni::JString, jni::JString>> {
          auto __map = jni::JHashMap<jni::JString, jni::JString>::create(value.headers.value().size());
          for (const auto& __entry : value.headers.value()) {
            __map->put(jni::make_jstring(__entry.first), jni::make_jstring(__entry.second));
          }
          return __map;
        }() : nullptr,
        value.query.has_value() ? [&]() -> jni::local_ref<jni::JMap<jni::JString, jni::JString>> {
          auto __map = jni::JHashMap<jni::JString, jni::JString>::create(value.query.value().size());
          for (const auto& __entry : value.query.value()) {
            __map->put(jni::make_jstring(__entry.first), jni::make_jstring(__entry.second));
          }
          return __map;
        }() : nullptr
      );
    }
  };

} // namespace margelo::nitro::audiobrowser
