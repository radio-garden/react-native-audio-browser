///
/// JRemoteButtonLayout.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#include <fbjni/fbjni.h>
#include "RemoteButtonLayout.hpp"

#include "JRemoteButton.hpp"
#include "RemoteButton.hpp"
#include <optional>
#include <vector>

namespace margelo::nitro::audiobrowser {

  using namespace facebook;

  /**
   * The C++ JNI bridge between the C++ struct "RemoteButtonLayout" and the the Kotlin data class "RemoteButtonLayout".
   */
  struct JRemoteButtonLayout final: public jni::JavaClass<JRemoteButtonLayout> {
  public:
    static auto constexpr kJavaDescriptor = "Lcom/margelo/nitro/audiobrowser/RemoteButtonLayout;";

  public:
    /**
     * Convert this Java/Kotlin-based struct to the C++ struct RemoteButtonLayout by copying all values to C++.
     */
    [[maybe_unused]]
    [[nodiscard]]
    RemoteButtonLayout toCpp() const {
      static const auto clazz = javaClassStatic();
      static const auto fieldBack = clazz->getField<JRemoteButton>("back");
      jni::local_ref<JRemoteButton> back = this->getFieldValue(fieldBack);
      static const auto fieldForward = clazz->getField<JRemoteButton>("forward");
      jni::local_ref<JRemoteButton> forward = this->getFieldValue(fieldForward);
      static const auto fieldOverflow = clazz->getField<jni::JArrayClass<JRemoteButton>>("overflow");
      jni::local_ref<jni::JArrayClass<JRemoteButton>> overflow = this->getFieldValue(fieldOverflow);
      return RemoteButtonLayout(
        back != nullptr ? std::make_optional(back->toCpp()) : std::nullopt,
        forward != nullptr ? std::make_optional(forward->toCpp()) : std::nullopt,
        overflow != nullptr ? std::make_optional([&]() {
          size_t __size = overflow->size();
          std::vector<RemoteButton> __vector;
          __vector.reserve(__size);
          for (size_t __i = 0; __i < __size; __i++) {
            auto __element = overflow->getElement(__i);
            __vector.push_back(__element->toCpp());
          }
          return __vector;
        }()) : std::nullopt
      );
    }

  public:
    /**
     * Create a Java/Kotlin-based struct by copying all values from the given C++ struct to Java.
     */
    [[maybe_unused]]
    static jni::local_ref<JRemoteButtonLayout::javaobject> fromCpp(const RemoteButtonLayout& value) {
      using JSignature = JRemoteButtonLayout(jni::alias_ref<JRemoteButton>, jni::alias_ref<JRemoteButton>, jni::alias_ref<jni::JArrayClass<JRemoteButton>>);
      static const auto clazz = javaClassStatic();
      static const auto create = clazz->getStaticMethod<JSignature>("fromCpp");
      return create(
        clazz,
        value.back.has_value() ? JRemoteButton::fromCpp(value.back.value()) : nullptr,
        value.forward.has_value() ? JRemoteButton::fromCpp(value.forward.value()) : nullptr,
        value.overflow.has_value() ? [&]() {
          size_t __size = value.overflow.value().size();
          jni::local_ref<jni::JArrayClass<JRemoteButton>> __array = jni::JArrayClass<JRemoteButton>::newArray(__size);
          for (size_t __i = 0; __i < __size; __i++) {
            const auto& __element = value.overflow.value()[__i];
            auto __elementJni = JRemoteButton::fromCpp(__element);
            __array->setElement(__i, *__elementJni);
          }
          return __array;
        }() : nullptr
      );
    }
  };

} // namespace margelo::nitro::audiobrowser
