///
/// AndroidOptions.swift
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

import NitroModules

/**
 * Represents an instance of `AndroidOptions`, backed by a C++ struct.
 */
public typealias AndroidOptions = margelo.nitro.audiobrowser.AndroidOptions

public extension AndroidOptions {
  private typealias bridge = margelo.nitro.audiobrowser.bridge.swift

  /**
   * Create a new instance of `AndroidOptions`.
   */
  init(appKilledPlaybackBehavior: AppKilledPlaybackBehavior, skipSilence: Bool, shuffle: Bool, ratingType: RatingType, notificationCapabilities: [Capability]?) {
    self.init(appKilledPlaybackBehavior, skipSilence, shuffle, ratingType, { () -> bridge.std__optional_std__vector_Capability__ in
      if let __unwrappedValue = notificationCapabilities {
        return bridge.create_std__optional_std__vector_Capability__({ () -> bridge.std__vector_Capability_ in
          var __vector = bridge.create_std__vector_Capability_(__unwrappedValue.count)
          for __item in __unwrappedValue {
            __vector.push_back(__item)
          }
          return __vector
        }())
      } else {
        return .init()
      }
    }())
  }

  var appKilledPlaybackBehavior: AppKilledPlaybackBehavior {
    @inline(__always)
    get {
      return self.__appKilledPlaybackBehavior
    }
    @inline(__always)
    set {
      self.__appKilledPlaybackBehavior = newValue
    }
  }
  
  var skipSilence: Bool {
    @inline(__always)
    get {
      return self.__skipSilence
    }
    @inline(__always)
    set {
      self.__skipSilence = newValue
    }
  }
  
  var shuffle: Bool {
    @inline(__always)
    get {
      return self.__shuffle
    }
    @inline(__always)
    set {
      self.__shuffle = newValue
    }
  }
  
  var ratingType: RatingType {
    @inline(__always)
    get {
      return self.__ratingType
    }
    @inline(__always)
    set {
      self.__ratingType = newValue
    }
  }
  
  var notificationCapabilities: [Capability]? {
    @inline(__always)
    get {
      return { () -> [Capability]? in
        if bridge.has_value_std__optional_std__vector_Capability__(self.__notificationCapabilities) {
          let __unwrapped = bridge.get_std__optional_std__vector_Capability__(self.__notificationCapabilities)
          return __unwrapped.map({ __item in __item })
        } else {
          return nil
        }
      }()
    }
    @inline(__always)
    set {
      self.__notificationCapabilities = { () -> bridge.std__optional_std__vector_Capability__ in
        if let __unwrappedValue = newValue {
          return bridge.create_std__optional_std__vector_Capability__({ () -> bridge.std__vector_Capability_ in
            var __vector = bridge.create_std__vector_Capability_(__unwrappedValue.count)
            for __item in __unwrappedValue {
              __vector.push_back(__item)
            }
            return __vector
          }())
        } else {
          return .init()
        }
      }()
    }
  }
}
