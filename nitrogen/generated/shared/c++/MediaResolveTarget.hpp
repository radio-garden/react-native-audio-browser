///
/// MediaResolveTarget.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::audiobrowser {

  /**
   * An enum which can be represented as a JavaScript union (MediaResolveTarget).
   */
  enum class MediaResolveTarget {
    LOCAL      SWIFT_NAME(local) = 0,
    CAST      SWIFT_NAME(cast) = 1,
  } CLOSED_ENUM;

} // namespace margelo::nitro::audiobrowser

namespace margelo::nitro {

  // C++ MediaResolveTarget <> JS MediaResolveTarget (union)
  template <>
  struct JSIConverter<margelo::nitro::audiobrowser::MediaResolveTarget> final {
    static inline margelo::nitro::audiobrowser::MediaResolveTarget fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("local"): return margelo::nitro::audiobrowser::MediaResolveTarget::LOCAL;
        case hashString("cast"): return margelo::nitro::audiobrowser::MediaResolveTarget::CAST;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum MediaResolveTarget - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::audiobrowser::MediaResolveTarget arg) {
      switch (arg) {
        case margelo::nitro::audiobrowser::MediaResolveTarget::LOCAL: return JSIConverter<std::string>::toJSI(runtime, "local");
        case margelo::nitro::audiobrowser::MediaResolveTarget::CAST: return JSIConverter<std::string>::toJSI(runtime, "cast");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert MediaResolveTarget to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("local"):
        case hashString("cast"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
