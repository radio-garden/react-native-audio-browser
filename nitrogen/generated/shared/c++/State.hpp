///
/// State.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::audiobrowser {

  /**
   * An enum which can be represented as a JavaScript union (State).
   */
  enum class State {
    NONE      SWIFT_NAME(none) = 0,
    READY      SWIFT_NAME(ready) = 1,
    PLAYING      SWIFT_NAME(playing) = 2,
    PAUSED      SWIFT_NAME(paused) = 3,
    STOPPED      SWIFT_NAME(stopped) = 4,
    LOADING      SWIFT_NAME(loading) = 5,
    BUFFERING      SWIFT_NAME(buffering) = 6,
    ERROR      SWIFT_NAME(error) = 7,
    ENDED      SWIFT_NAME(ended) = 8,
  } CLOSED_ENUM;

} // namespace margelo::nitro::audiobrowser

namespace margelo::nitro {

  // C++ State <> JS State (union)
  template <>
  struct JSIConverter<margelo::nitro::audiobrowser::State> final {
    static inline margelo::nitro::audiobrowser::State fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("none"): return margelo::nitro::audiobrowser::State::NONE;
        case hashString("ready"): return margelo::nitro::audiobrowser::State::READY;
        case hashString("playing"): return margelo::nitro::audiobrowser::State::PLAYING;
        case hashString("paused"): return margelo::nitro::audiobrowser::State::PAUSED;
        case hashString("stopped"): return margelo::nitro::audiobrowser::State::STOPPED;
        case hashString("loading"): return margelo::nitro::audiobrowser::State::LOADING;
        case hashString("buffering"): return margelo::nitro::audiobrowser::State::BUFFERING;
        case hashString("error"): return margelo::nitro::audiobrowser::State::ERROR;
        case hashString("ended"): return margelo::nitro::audiobrowser::State::ENDED;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum State - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::audiobrowser::State arg) {
      switch (arg) {
        case margelo::nitro::audiobrowser::State::NONE: return JSIConverter<std::string>::toJSI(runtime, "none");
        case margelo::nitro::audiobrowser::State::READY: return JSIConverter<std::string>::toJSI(runtime, "ready");
        case margelo::nitro::audiobrowser::State::PLAYING: return JSIConverter<std::string>::toJSI(runtime, "playing");
        case margelo::nitro::audiobrowser::State::PAUSED: return JSIConverter<std::string>::toJSI(runtime, "paused");
        case margelo::nitro::audiobrowser::State::STOPPED: return JSIConverter<std::string>::toJSI(runtime, "stopped");
        case margelo::nitro::audiobrowser::State::LOADING: return JSIConverter<std::string>::toJSI(runtime, "loading");
        case margelo::nitro::audiobrowser::State::BUFFERING: return JSIConverter<std::string>::toJSI(runtime, "buffering");
        case margelo::nitro::audiobrowser::State::ERROR: return JSIConverter<std::string>::toJSI(runtime, "error");
        case margelo::nitro::audiobrowser::State::ENDED: return JSIConverter<std::string>::toJSI(runtime, "ended");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert State to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("none"):
        case hashString("ready"):
        case hashString("playing"):
        case hashString("paused"):
        case hashString("stopped"):
        case hashString("loading"):
        case hashString("buffering"):
        case hashString("error"):
        case hashString("ended"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
