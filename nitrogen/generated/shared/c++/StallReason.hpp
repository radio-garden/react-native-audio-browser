///
/// StallReason.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::audiobrowser {

  /**
   * An enum which can be represented as a JavaScript union (StallReason).
   */
  enum class StallReason {
    BUFFERING      SWIFT_NAME(buffering) = 0,
    OFFLINE      SWIFT_NAME(offline) = 1,
  } CLOSED_ENUM;

} // namespace margelo::nitro::audiobrowser

namespace margelo::nitro {

  // C++ StallReason <> JS StallReason (union)
  template <>
  struct JSIConverter<margelo::nitro::audiobrowser::StallReason> final {
    static inline margelo::nitro::audiobrowser::StallReason fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("buffering"): return margelo::nitro::audiobrowser::StallReason::BUFFERING;
        case hashString("offline"): return margelo::nitro::audiobrowser::StallReason::OFFLINE;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum StallReason - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::audiobrowser::StallReason arg) {
      switch (arg) {
        case margelo::nitro::audiobrowser::StallReason::BUFFERING: return JSIConverter<std::string>::toJSI(runtime, "buffering");
        case margelo::nitro::audiobrowser::StallReason::OFFLINE: return JSIConverter<std::string>::toJSI(runtime, "offline");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert StallReason to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("buffering"):
        case hashString("offline"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
