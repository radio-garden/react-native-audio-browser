///
/// CardImage.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::audiobrowser {

  /**
   * An enum which can be represented as a JavaScript union (CardImage).
   */
  enum class CardImage {
    NORMAL      SWIFT_NAME(normal) = 0,
    BACKGROUND      SWIFT_NAME(background) = 1,
  } CLOSED_ENUM;

} // namespace margelo::nitro::audiobrowser

namespace margelo::nitro {

  // C++ CardImage <> JS CardImage (union)
  template <>
  struct JSIConverter<margelo::nitro::audiobrowser::CardImage> final {
    static inline margelo::nitro::audiobrowser::CardImage fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("normal"): return margelo::nitro::audiobrowser::CardImage::NORMAL;
        case hashString("background"): return margelo::nitro::audiobrowser::CardImage::BACKGROUND;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum CardImage - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::audiobrowser::CardImage arg) {
      switch (arg) {
        case margelo::nitro::audiobrowser::CardImage::NORMAL: return JSIConverter<std::string>::toJSI(runtime, "normal");
        case margelo::nitro::audiobrowser::CardImage::BACKGROUND: return JSIConverter<std::string>::toJSI(runtime, "background");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert CardImage to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("normal"):
        case hashString("background"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
