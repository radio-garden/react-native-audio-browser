///
/// OutputType.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::audiobrowser {

  /**
   * An enum which can be represented as a JavaScript union (OutputType).
   */
  enum class OutputType {
    SPEAKER      SWIFT_NAME(speaker) = 0,
    RECEIVER      SWIFT_NAME(receiver) = 1,
    HEADPHONES      SWIFT_NAME(headphones) = 2,
    BLUETOOTH      SWIFT_NAME(bluetooth) = 3,
    AIRPLAY      SWIFT_NAME(airplay) = 4,
    CAR      SWIFT_NAME(car) = 5,
    HDMI      SWIFT_NAME(hdmi) = 6,
    USB      SWIFT_NAME(usb) = 7,
    CAST      SWIFT_NAME(cast) = 8,
    OTHER      SWIFT_NAME(other) = 9,
  } CLOSED_ENUM;

} // namespace margelo::nitro::audiobrowser

namespace margelo::nitro {

  // C++ OutputType <> JS OutputType (union)
  template <>
  struct JSIConverter<margelo::nitro::audiobrowser::OutputType> final {
    static inline margelo::nitro::audiobrowser::OutputType fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("speaker"): return margelo::nitro::audiobrowser::OutputType::SPEAKER;
        case hashString("receiver"): return margelo::nitro::audiobrowser::OutputType::RECEIVER;
        case hashString("headphones"): return margelo::nitro::audiobrowser::OutputType::HEADPHONES;
        case hashString("bluetooth"): return margelo::nitro::audiobrowser::OutputType::BLUETOOTH;
        case hashString("airplay"): return margelo::nitro::audiobrowser::OutputType::AIRPLAY;
        case hashString("car"): return margelo::nitro::audiobrowser::OutputType::CAR;
        case hashString("hdmi"): return margelo::nitro::audiobrowser::OutputType::HDMI;
        case hashString("usb"): return margelo::nitro::audiobrowser::OutputType::USB;
        case hashString("cast"): return margelo::nitro::audiobrowser::OutputType::CAST;
        case hashString("other"): return margelo::nitro::audiobrowser::OutputType::OTHER;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum OutputType - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::audiobrowser::OutputType arg) {
      switch (arg) {
        case margelo::nitro::audiobrowser::OutputType::SPEAKER: return JSIConverter<std::string>::toJSI(runtime, "speaker");
        case margelo::nitro::audiobrowser::OutputType::RECEIVER: return JSIConverter<std::string>::toJSI(runtime, "receiver");
        case margelo::nitro::audiobrowser::OutputType::HEADPHONES: return JSIConverter<std::string>::toJSI(runtime, "headphones");
        case margelo::nitro::audiobrowser::OutputType::BLUETOOTH: return JSIConverter<std::string>::toJSI(runtime, "bluetooth");
        case margelo::nitro::audiobrowser::OutputType::AIRPLAY: return JSIConverter<std::string>::toJSI(runtime, "airplay");
        case margelo::nitro::audiobrowser::OutputType::CAR: return JSIConverter<std::string>::toJSI(runtime, "car");
        case margelo::nitro::audiobrowser::OutputType::HDMI: return JSIConverter<std::string>::toJSI(runtime, "hdmi");
        case margelo::nitro::audiobrowser::OutputType::USB: return JSIConverter<std::string>::toJSI(runtime, "usb");
        case margelo::nitro::audiobrowser::OutputType::CAST: return JSIConverter<std::string>::toJSI(runtime, "cast");
        case margelo::nitro::audiobrowser::OutputType::OTHER: return JSIConverter<std::string>::toJSI(runtime, "other");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert OutputType to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("speaker"):
        case hashString("receiver"):
        case hashString("headphones"):
        case hashString("bluetooth"):
        case hashString("airplay"):
        case hashString("car"):
        case hashString("hdmi"):
        case hashString("usb"):
        case hashString("cast"):
        case hashString("other"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
