///
/// PlaybackErrorKind.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::audiobrowser {

  /**
   * An enum which can be represented as a JavaScript union (PlaybackErrorKind).
   */
  enum class PlaybackErrorKind {
    UNKNOWN      SWIFT_NAME(unknown) = 0,
    OFFLINE      SWIFT_NAME(offline) = 1,
    UNREACHABLE      SWIFT_NAME(unreachable) = 2,
    NOT_FOUND      SWIFT_NAME(notFound) = 3,
    REJECTED      SWIFT_NAME(rejected) = 4,
    SERVER_ERROR      SWIFT_NAME(serverError) = 5,
    UNPLAYABLE      SWIFT_NAME(unplayable) = 6,
    STALLED      SWIFT_NAME(stalled) = 7,
  } CLOSED_ENUM;

} // namespace margelo::nitro::audiobrowser

namespace margelo::nitro {

  // C++ PlaybackErrorKind <> JS PlaybackErrorKind (union)
  template <>
  struct JSIConverter<margelo::nitro::audiobrowser::PlaybackErrorKind> final {
    static inline margelo::nitro::audiobrowser::PlaybackErrorKind fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("unknown"): return margelo::nitro::audiobrowser::PlaybackErrorKind::UNKNOWN;
        case hashString("offline"): return margelo::nitro::audiobrowser::PlaybackErrorKind::OFFLINE;
        case hashString("unreachable"): return margelo::nitro::audiobrowser::PlaybackErrorKind::UNREACHABLE;
        case hashString("not-found"): return margelo::nitro::audiobrowser::PlaybackErrorKind::NOT_FOUND;
        case hashString("rejected"): return margelo::nitro::audiobrowser::PlaybackErrorKind::REJECTED;
        case hashString("server-error"): return margelo::nitro::audiobrowser::PlaybackErrorKind::SERVER_ERROR;
        case hashString("unplayable"): return margelo::nitro::audiobrowser::PlaybackErrorKind::UNPLAYABLE;
        case hashString("stalled"): return margelo::nitro::audiobrowser::PlaybackErrorKind::STALLED;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum PlaybackErrorKind - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::audiobrowser::PlaybackErrorKind arg) {
      switch (arg) {
        case margelo::nitro::audiobrowser::PlaybackErrorKind::UNKNOWN: return JSIConverter<std::string>::toJSI(runtime, "unknown");
        case margelo::nitro::audiobrowser::PlaybackErrorKind::OFFLINE: return JSIConverter<std::string>::toJSI(runtime, "offline");
        case margelo::nitro::audiobrowser::PlaybackErrorKind::UNREACHABLE: return JSIConverter<std::string>::toJSI(runtime, "unreachable");
        case margelo::nitro::audiobrowser::PlaybackErrorKind::NOT_FOUND: return JSIConverter<std::string>::toJSI(runtime, "not-found");
        case margelo::nitro::audiobrowser::PlaybackErrorKind::REJECTED: return JSIConverter<std::string>::toJSI(runtime, "rejected");
        case margelo::nitro::audiobrowser::PlaybackErrorKind::SERVER_ERROR: return JSIConverter<std::string>::toJSI(runtime, "server-error");
        case margelo::nitro::audiobrowser::PlaybackErrorKind::UNPLAYABLE: return JSIConverter<std::string>::toJSI(runtime, "unplayable");
        case margelo::nitro::audiobrowser::PlaybackErrorKind::STALLED: return JSIConverter<std::string>::toJSI(runtime, "stalled");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert PlaybackErrorKind to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("unknown"):
        case hashString("offline"):
        case hashString("unreachable"):
        case hashString("not-found"):
        case hashString("rejected"):
        case hashString("server-error"):
        case hashString("unplayable"):
        case hashString("stalled"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
