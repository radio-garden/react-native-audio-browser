///
/// TrackType.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::audiobrowser {

  /**
   * An enum which can be represented as a JavaScript union (TrackType).
   */
  enum class TrackType {
    DEFAULT      SWIFT_NAME(default) = 0,
    DASH      SWIFT_NAME(dash) = 1,
    HLS      SWIFT_NAME(hls) = 2,
    SMOOTHSTREAMING      SWIFT_NAME(smoothstreaming) = 3,
  } CLOSED_ENUM;

} // namespace margelo::nitro::audiobrowser

namespace margelo::nitro {

  // C++ TrackType <> JS TrackType (union)
  template <>
  struct JSIConverter<margelo::nitro::audiobrowser::TrackType> final {
    static inline margelo::nitro::audiobrowser::TrackType fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("default"): return margelo::nitro::audiobrowser::TrackType::DEFAULT;
        case hashString("dash"): return margelo::nitro::audiobrowser::TrackType::DASH;
        case hashString("hls"): return margelo::nitro::audiobrowser::TrackType::HLS;
        case hashString("smoothstreaming"): return margelo::nitro::audiobrowser::TrackType::SMOOTHSTREAMING;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum TrackType - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::audiobrowser::TrackType arg) {
      switch (arg) {
        case margelo::nitro::audiobrowser::TrackType::DEFAULT: return JSIConverter<std::string>::toJSI(runtime, "default");
        case margelo::nitro::audiobrowser::TrackType::DASH: return JSIConverter<std::string>::toJSI(runtime, "dash");
        case margelo::nitro::audiobrowser::TrackType::HLS: return JSIConverter<std::string>::toJSI(runtime, "hls");
        case margelo::nitro::audiobrowser::TrackType::SMOOTHSTREAMING: return JSIConverter<std::string>::toJSI(runtime, "smoothstreaming");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert TrackType to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("default"):
        case hashString("dash"):
        case hashString("hls"):
        case hashString("smoothstreaming"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
