///
/// FavoritesMatchMode.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::audiobrowser {

  /**
   * An enum which can be represented as a JavaScript union (FavoritesMatchMode).
   */
  enum class FavoritesMatchMode {
    EXACT      SWIFT_NAME(exact) = 0,
    PARTIAL      SWIFT_NAME(partial) = 1,
  } CLOSED_ENUM;

} // namespace margelo::nitro::audiobrowser

namespace margelo::nitro {

  // C++ FavoritesMatchMode <> JS FavoritesMatchMode (union)
  template <>
  struct JSIConverter<margelo::nitro::audiobrowser::FavoritesMatchMode> final {
    static inline margelo::nitro::audiobrowser::FavoritesMatchMode fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("exact"): return margelo::nitro::audiobrowser::FavoritesMatchMode::EXACT;
        case hashString("partial"): return margelo::nitro::audiobrowser::FavoritesMatchMode::PARTIAL;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum FavoritesMatchMode - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::audiobrowser::FavoritesMatchMode arg) {
      switch (arg) {
        case margelo::nitro::audiobrowser::FavoritesMatchMode::EXACT: return JSIConverter<std::string>::toJSI(runtime, "exact");
        case margelo::nitro::audiobrowser::FavoritesMatchMode::PARTIAL: return JSIConverter<std::string>::toJSI(runtime, "partial");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert FavoritesMatchMode to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("exact"):
        case hashString("partial"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
