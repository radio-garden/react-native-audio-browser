# Next.js Example

Demonstrates `react-native-audio-browser` on the web with Next.js, React Native Web, and Shaka Player.

## Quick Start

```bash
# From the monorepo root
yarn install
yarn prepare
cd apps/example-nextjs
yarn dev
```

Open [http://localhost:3003](http://localhost:3003).
